Sum of Squares of Distinct Counts in Subarrays

def sum_sq_distinct(nums):
    n = len(nums)
    ans = 0

    for i in range(n):
        seen = set()
        for j in range(i, n):
            seen.add(nums[j])
            ans += len(seen) * len(seen)

    return ans